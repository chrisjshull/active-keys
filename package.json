{
  "name": "active-keys",
  "version": "0.0.1",
  "description": "Know and listen for which keys the user is currently holding down.",
  "main": "dist/",
  "scripts": {
    "build": "babel ./src --presets babel-preset-es2015,babel-preset-stage-2 --out-dir dist",
    "prepublishOnly": "npm run build",
    "preversion": "git diff-index --quiet HEAD --"
  },
  "author": "chris@dremio.com",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  },
  "dependencies": {
    "event-target-shim": "^2.0.0"
  }
}
